<mapper xmlns="http://mybatis.org/schema/mybatis-mapper">
    <select id="retrieveAll" resultType="com.example.mybatis.Mybatistest.domain.User">
        SELECT user.* FROM user
    </select>
</mapper>